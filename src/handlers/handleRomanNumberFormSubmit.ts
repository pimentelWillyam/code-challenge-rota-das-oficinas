
export const handleRomanNumberFormSubmit = (event: React.FormEvent<HTMLFormElement> ,conversionFormat: string, numberToBeConverted: string | null, setConvertedNumber: React.Dispatch<React.SetStateAction<null>> ) =>{
   event.preventDefault()
   if (conversionFormat == 'Arabic to roman'){

   }
   else if (conversionFormat == 'Roman to arabic'){
      
   }
}